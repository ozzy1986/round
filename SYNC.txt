5-PLACE SYNC — FOR AI / NEXT SESSION
=====================================
After any task that changes code or config, sync to: THIS PC, GITHUB, GITLAB, VPS, PHONE.
Read this file to perform the full sync.

REPOSITORY
----------
  Branch:    main
  Origin:    https://github.com/ozzy1986/round.git   (git push origin main)
  GitLab:    https://gitlab.com/ozzy1986/round.git   (git push gitlab main)

VPS
---
  Host:      root@162.246.19.78
  App dir:   /var/www/round.ozzy1986.com
  SSH key from this machine to VPS must be set up (passwordless). .env lives on VPS only; not in repo; not overwritten on deploy.

EXACT STEPS (run from project root, e.g. d:\laragon\www\raund)
--------------------------------------------------------------
1. This PC (local)
   - Edit files, run tests (npm test), run migrations if needed (npm run migrate:up).
   - If android/ changed: run build and QA gate before commit:
     .\scripts\build-apk.ps1
     .\gradlew.bat :app:lintDebug
     Then run on-device checklist from ANDROID_TESTING.txt.
   - Stage and commit:
     git add -A
     git commit -m "Your message"

2. Push to GitHub
   git push origin main

3. Push to GitLab
   git push gitlab main

4. Deploy on VPS (SSH and run deploy script)
   ssh -o BatchMode=yes root@162.246.19.78 "cd /var/www/round.ozzy1986.com && bash scripts/deploy-vps.sh"

   This does: git pull, npm ci, npm run migrate:up, npm run build, pm2 restart round-api, pm2 restart round-bot, pm2 save.

5. Phone (only if android/ changed)
   - For local phone QA, always use the optimized local release APK:
     android/app/build/outputs/apk/localRelease/app-localRelease.apk
     (debug builds are 3-4x slower due to unoptimized DEX, no R8, no AOT).
     For store/publishing builds, use android/app/build/outputs/apk/release/app-release.apk.
   - Install via ADB:
     & "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe" install -r android\app\build\outputs\apk\localRelease\app-localRelease.apk
   - Or transfer via USB/cloud/email; install on device (enable "Install from unknown sources" if prompted).
   - If android/ did not change, phone is already in sync (app talks to VPS API).

6. Update text docs so they reflect the change:
   - PROJECT_DESCRIPTION.txt (always)
   - ANDROID_TESTING.txt (when test flow/checklist changed)
   - Any other operational .txt docs touched by this task

DONE. All five (this PC, GitHub, GitLab, VPS, phone) are in sync, and the project description is up to date.

NOTES
-----
- Do not commit .env. It is in .gitignore. Use .env.example as template (no real credentials in repo).
- To change secrets on VPS (e.g. TELEGRAM_BOT_TOKEN), edit /var/www/round.ozzy1986.com/.env on the server (e.g. via SSH), then restart: ssh root@162.246.19.78 "pm2 restart round-api round-bot".
- Optional VPS cron for refresh token cleanup: 0 3 * * * cd /var/www/round.ozzy1986.com && node dist/scripts/purge-tokens.js
- One-time VPS setup (clone, Nginx, PM2): see ENV_CHECK.txt section "VPS DEPLOY".
